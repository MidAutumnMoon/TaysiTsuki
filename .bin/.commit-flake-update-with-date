#!/usr/bin/env nu

def main [] {
    let toplevel = (^git rev-parse --show-toplevel | str trim)
    let date_str = (date now | format date "%Y/%b/%d")
    let commit_msg = $"flake update: ($date_str)"

    ^git commit -m $commit_msg -- $"($toplevel)/flake.lock"
}
